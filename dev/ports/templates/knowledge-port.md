---
name: <port 名。ツリー内で一意。ファイル名と一致させる>
description: <1 行の要約>
inject:
  - <注入先スキル(dev-implement / dev-spec / dev-decompose / dev-check / dev-release 等)>
condition: <常時、または「〜を含む作業のとき」等の判定可能な自然言語条件>
---

# <タイトル>(<name>)

<!-- 知識 port の雛形。docs/dev/ports/<ジャンル>/<name>.md にコピーし、frontmatter と本文を
     プロジェクトの知識で置き換えて使う。この HTML コメントは削除する。
     知識 port = 作業内容に応じて条件付きで部品へ注入する専門知識(言語規約・機能ドメイン・
     視覚設計・原則等)。正本は利用側プロジェクトに置き Git 管理する(ports.md §4)。 -->

<この port の目的を 1〜2 文で。何のためにどの部品へ何を注入するか。出典があれば添える。>

## 1. <観点・項目>

- <規範・知見を箇条書きで。注入のたびにコンテキストを消費するため簡潔に保つ。>

## 2. <観点・項目>

- <...>
